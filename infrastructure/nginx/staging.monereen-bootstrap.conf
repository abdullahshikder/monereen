server {
    listen 80;
    listen [::]:80;
    server_name staging.monereen.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/monereen-acme;
    }

    location / {
        return 503 "Staging HTTPS setup in progress.\n";
    }
}

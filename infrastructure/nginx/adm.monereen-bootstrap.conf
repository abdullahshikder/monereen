server {
    listen 80;
    listen [::]:80;
    server_name adm.monereen.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/monereen-acme;
    }

    location / {
        return 503 "Admin portal HTTPS setup in progress.\n";
    }
}
